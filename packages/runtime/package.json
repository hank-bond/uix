{
  "name": "@uix/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Exactly one workspace's substrate semantics: the accepted feature composition, registries, stores, surface delivery, reload coordination, and agent instances behind one WorkspaceRuntime. Source-only local package.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ]
}
