{
  "name": "@uix/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Exactly one workspace's substrate semantics: the accepted feature composition, registries, stores, and agent instances behind one WorkspaceHandle. Source-only local package until H3 fills it.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ]
}
