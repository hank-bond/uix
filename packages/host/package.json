{
  "name": "@uix/host",
  "version": "0.0.0",
  "private": true,
  "description": "Shared host contracts and coordination: the workspace supervisor, workspace handles, and the launcher/catalog projection schemas that concrete hosts compose. Source-only local package; an in-memory suite proved the supervisor, handle, and attachment contracts.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "types": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ]
}
