// Trellis cockpit — renderer entry.
// Vanilla TypeScript for now; React lands in commit 4.
// Logging from here just proves the bundler picked up the module and HMR works.
console.log("[trellis] renderer booted");

export {};
