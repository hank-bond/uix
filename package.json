{
  "name": "trellis",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal protocol + cockpit for agent ↔ HTML ↔ human interfaces.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "//": "scaffolding lands in commit 2"
  }
}
